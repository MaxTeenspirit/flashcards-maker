import {Box} from '@chakra-ui/react';

import {IRoundImage} from './IRoundImage.ts';

const RoundImage = ({imageName, sizes}: IRoundImage) => {
	return <Box>RoundImage</Box>;
};

export default RoundImage;
